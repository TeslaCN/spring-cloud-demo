spring:
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: client-demo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:30000/eureka/
server:
  port: 50001

